# code_virtualizer

Standalone PE code virtualizer / binary rewriter.

Build the solution with Visual Studio or MSBuild:

```powershell
MSBuild.exe .\code_virtualizer.sln /p:Configuration=Release /p:Platform=x64
```
